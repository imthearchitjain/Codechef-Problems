#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int t;
    cin >> t;
    int a;
    while (t--)
    {
        cin >> a;
        int result = 0;
        result = sqrt(a);
        cout << result << endl;
    }
    return 0;
}
